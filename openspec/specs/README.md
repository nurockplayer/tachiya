# OpenSpec Specs

This directory contains accepted living behavior specs.

Do not write proposed behavior here directly. Start in `openspec/changes/<change-id>/specs/`, review the proposal and tasks, then sync / archive the change after acceptance.

Specs should describe user-observable or system-observable behavior and use scenario-oriented language such as Given / When / Then.
