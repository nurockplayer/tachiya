[project]
name = "tachiya-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "httpx>=0.27",        # Saleor GraphQL 呼叫
    "python-dotenv>=1.0",
]
