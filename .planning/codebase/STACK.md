# STACK.md — Technology Stack

_Generated by gsd-codebase-mapper (tech focus)_

---

## Overview

Tachiya is a multi-service e-commerce platform built on top of Saleor, with a custom FastAPI layer for business logic.

---

## Services

| Service | Language / Runtime | Framework | Port |
|---------|-------------------|-----------|------|
| saleor | Python 3.x | Django + Saleor 3.22 | 8000 |
| api (Tachiya) | Python 3.12 | FastAPI 0.115+ | 8001 |
| dashboard | TypeScript / Node | React + Saleor Dashboard 3.22 | 9000 |
| frontend | TypeScript / Node | Next.js 16 + React 19 | 3000 |
| db | — | PostgreSQL 16 | 5432 |
| cache | — | Valkey 8.1 (Redis-compatible) | 6379 |
| mailpit | — | Mailpit (dev SMTP) | 1025/8025 |

---

## Tachiya API (`api/`)

- **Language:** Python 3.12
- **Framework:** FastAPI 0.115+
- **ASGI Server:** Uvicorn (standard extras — includes uvloop, websockets)
- **HTTP Client:** httpx 0.27 (for Saleor GraphQL calls)
- **Config:** python-dotenv
- **Package Manager:** uv (pyproject.toml, PEP 517)
- **Status:** Early scaffold — only `/health` endpoint exists

### Key Dependencies
```
fastapi>=0.115
uvicorn[standard]>=0.32
httpx>=0.27
python-dotenv>=1.0
```

---

## Frontend (`frontend/`)

- **Framework:** Next.js 16.1.2 (App Router)
- **Language:** TypeScript
- **React:** 19.1.2
- **Package Manager:** pnpm (enforced via `only-allow`)
- **Styling:** Tailwind CSS + tailwind-merge + tailwindcss-animate
- **GraphQL:** urql 4 + graphql-codegen (typed document nodes)
- **Auth:** @saleor/auth-sdk 1.0.3
- **i18n:** next-intl 4.8.3
- **UI Primitives:** @headlessui/react, @radix-ui/react-*
- **Icons:** lucide-react
- **Testing:** Vitest 4
- **Linting:** ESLint
- **Git Hooks:** Husky
- **Dead code:** Knip

---

## Dashboard (`dashboard/`)

- **Base:** Saleor Dashboard 3.22 (forked, Traditional Chinese localization)
- **Language:** TypeScript
- **Framework:** React
- **Package Manager:** pnpm
- **Build Image:** `ghcr.io/nurockplayer/tachiya-dashboard:3.22` (custom build)
- **Testing:** Jest + SWC (unit), Playwright (E2E)
- **CI Hooks:** Custom `.claude/` hooks for code quality

---

## Infrastructure

- **Containerization:** Docker + Docker Compose
- **Saleor Image:** `ghcr.io/saleor/saleor:3.22`
- **Background Tasks:** Celery (via saleor-worker service)
- **Database:** PostgreSQL 16
- **Cache/Broker:** Valkey 8.1 (Redis-compatible)
- **Dev Email:** Mailpit (captures outgoing SMTP)
- **Build Orchestration:** Makefile

---

## Deployment

- Dashboard served as static files via Nginx (inside container, port 80)
- Frontend optional (Docker profile: `frontend`)
- All services defined in `docker-compose.yml`
