# TESTING.md — Test Infrastructure & Coverage

_Generated by gsd-codebase-mapper (quality focus)_

---

## Overview

| Service | Framework | Type | Status |
|---------|-----------|------|--------|
| frontend | Vitest 4 | Unit | Active |
| dashboard | Jest + SWC | Unit | Active (421 files) |
| dashboard | Playwright | E2E | Active (27 specs) |
| api | — | — | No tests yet |

---

## Frontend (`frontend/`)

### Unit Tests — Vitest 4

- **Environment:** `node`
- **Test file location:** Co-located `.test.ts` / `.test.tsx` under `src/`
- **Fixtures pattern:** `__fixtures__/` directories
- **Mocking:** `vi.mock()` hoisting pattern
- **Path alias:** `@` → `./src`
- **Run command:** `pnpm test` (or `pnpm test:run` for CI)

---

## Dashboard (`dashboard/`)

### Unit Tests — Jest + SWC

- **Test runner:** Jest with SWC transformer (fast TypeScript compilation)
- **Environment:** JSDOM
- **Scale:** ~421 test files
- **Testing library:** `@testing-library/react`
- **Global setup:** `testUtils/` directory
- **Mocking patterns:** `jest.mock()`, `identity-obj-proxy` for CSS modules
- **Snapshots:** Used for UI regression
- **Coverage source:** All `src/**/*.{ts,tsx}`

### E2E Tests — Playwright

- **Spec files:** 27 spec files under `playwright/`
- **Page Object Model:** `playwright/pages/` directory
- **Selector convention:** `data-test-id` attributes
- **Test naming:** `TC: SALEOR_N` format
- **Permission scoping:** `testWithPermission` helper for role-based test isolation
- **CI environment:** Requires env vars (API URL, credentials)
- **Playwright config:** Custom hooks in `.claude/plugins/dashboard-playwright/`

---

## API (`api/`)

### Status: No Tests

- No test files exist currently
- **Recommended:** Add pytest + httpx for integration tests
- FastAPI's `TestClient` (from `httpx`) is the standard approach

---

## CI/CD

- No `.github/workflows/` directory found at the repo root level
- Dashboard has its own GitHub Actions under `dashboard/.github/workflows/`
- CI test commands likely: `pnpm test:run` (unit), Playwright runner for E2E

---

## Gaps & Recommendations

1. **api/** has zero test coverage — highest priority gap
2. No integration tests across service boundaries (api ↔ Saleor)
3. Dashboard E2E requires live Saleor instance (not unit-testable in isolation)
4. Frontend test coverage depth unknown — needs audit
